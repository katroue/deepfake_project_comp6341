# Strategy 4: Self-Supervised Learning Configuration

strategy_name: "Self-Supervised Pretraining"
strategy_description: "Two-phase: SSL on unlabeled faces then fine-tune on FF++"

# Data
data_root: "data/"
compression: "c23"

# Model
model: "efficientnet_b1"
num_classes: 2
pretrained: false  # Train from scratch in Phase 1
dropout: 0.3

# Phase 1: Self-Supervised Pretraining
phase_1:
  enabled: true
  method: "SimSiam"
  data_root: "data/"  # Uses FF++ faces (labels ignored in SSL pretraining)
  num_epochs: 20
  batch_size: 64
  learning_rate: 0.001
  projection_dim: 512
  pred_dim: 128
  augmentation: "strong"

# Phase 2: Supervised Fine-tuning
phase_2:
  enabled: true
  load_pretrained: true
  pretrained_path: "results/models/c23/strategy4_ssl/phase1_pretrained/best_model.pth"
  num_epochs: 20
  batch_size: 64
  learning_rate: 0.0001
  weight_decay: 0.0001
  num_workers: 8
  augmentation: "baseline"
  freeze_backbone_epochs: 0
  class_weight_real: 2.0  # reduced from 3.64 — original value caused oscillation/collapse

# Paths
save_dir: "results/models/c23/strategy4_ssl"
log_dir: "results/logs/c23"

# Device
device: "auto"  # auto-detect MPS/CUDA/CPU